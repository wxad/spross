import API from '@/components/message/API';
import Basic from '@/components/message/Basic';
import Collapsible from '@/components/message/Collapsible';
import SameCollapsible from '@/components/message/SameCollapsible';

# Message

Messages display brief messages for the user without interrupting their use of the app.

## Basic Usage

four intents:

<Basic />

## Collapsible

collapse messages by setting the `collapsible` property to `true` once. After that, all the messages will be collapsed.

<Collapsible />

## Same Collapsible

set the `sameCollapsible` property to `true` to collapse all the messages with the same intent.

<SameCollapsible />

## API

<API />
