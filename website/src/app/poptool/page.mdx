import Basics from '@/components/poptool/Basics';
import API from '@/components/poptool/API';
import Flip from '@/components/poptool/Flip';
import Controlled from '@/components/poptool/Controlled';
import Height from '@/components/poptool/Height';
import Overflow from '@/components/poptool/Overflow';

# Popover & Tooltip

Popover & Tooltips display informative text when users hover over, focus on, or tap an element.

## Basic Usage

`arrowed`, `placement` & `trigger` Props：

<Basics />

## Inner scrollable

no more `getPopupContainer` [in the past](https://ant.design/components/popover#api):

<Overflow />

## Flip & Shift

<Flip />

## Self-adaptive height

combine with `flip` to maximize space:

<Height />

## Controlled

`visible` & `onVisibleChange` Props:

<Controlled />

## API

<API />
